# Core ML
torch
torchvision
opencv-python
numpy
tqdm
scikit-image

# Export / alternative runtimes
onnx
onnxscript           # required by torch.onnx.export on torch >= 2.6
onnxruntime          # use onnxruntime-gpu on CUDA machines

# Worker / API client
requests

# Dev
pytest

# GPU-only extras (install on NVIDIA machines, e.g. T4):
#   onnxruntime-gpu
#   tensorrt
#   cuda-python
